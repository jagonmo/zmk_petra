if SHIELD_FLATCAT_DONGLE

config ZMK_KEYBOARD_NAME
	default "flatcat Dongle"
	
config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y
	
config ZMK_USB
	default y

endif

if SHIELD_FLATCAT_KB || SHIELD_FLATCAT_DONGLE 

config ZMK_SPLIT
	default y

config ZMK_BLE
	default y
	
endif